Merge Two Sorted Linked Lists

== Met bouwen van een nieuwe List ==

== Gebruik een temporary Vector == O(n) extra space

Maak een vector aan: dynamisch want lengte Linked Lists zijn nog niet bekend
Zet Pointers op head list1 en head list2
Check of een van beide lists niet leeg is
Doorloop beide linked lists met while loop en increment beide pointers.
Vergelijk de waarden van list1 en list2
- Als de waarde van list1 <= waarde list2, kopieer de waarde van list 1 naar 
de vector op [k]. verschuif pointer van list1 naar Rechts. Verschuif k
- Als de waarde van list2 <= waarde list1, kopieer de waarde van list 2 naar 
de vector op [k]. verschuif pointer van list1 naar Rechts. Verschuif k
- Als list 1 langer is dan list 2 (dat is als we bij list 2 het einde hebben bereikt)
kopieer het resterende gedeelte van list 1 naar de vector
- Als list 2 langer is dan list 1 (dat is als we bij list 1 het einde hebben bereikt)
kopieer het resterende gedeelte van list 2 naar de vector

Van Vector naar Linked List
- We kunnen de linked list 1 en 2 koppelen: Het einde van list 1 of list 2 aan het
begin van list 1 of list 2 (volgorde maakt niet uit want we gaan values overschrijven)
- pointer k van de vector staat achteraan de vector: Die kunnen we teruglaten lopen
(alhoewel dat kan niet want de linked list is singly) dus:
- Start pointer k vanaf begin en kopieer de waarden weer terug naar de linked list


== Of in 1 van delijsten ==
