// 8-4-2026: nog eens helemaal opnieuw doen. idee was goed maar toch niet helemaal goed.

#include <vector>
#include <stdlib.h>
#include <iostream>
#include <unordered_set>

using namespace std;

class Solution:
{
    public: 
        int LSWRC(string s)
        {
            //We hebben nodig: een linkerpointer, rechterpointer en maximum resultaat.
            int left, right, max = 0;

            //Initaliseer een hashset om de duplicaten te vinden
            //Als een duplicaat in de hashset zit, dan verschuiven we de linkerpointer.
            //Dit checken we meerdere keren: bijv [a,b,c,b]. 
            //Per letter checken we de hashset. Komen we bij de 2e b aan,
            //dan checken we die in de hashset: zit erin. Dus verschuif linkerpointer van a naar b
            //dan checken we weer de hashset. Daar blijkt die dan nog steeds in te zitten: dus nogmaals
            //verschuiven van links.
            unordered_set<char>seen;

            //whileloop: schuiven conditioneel

            // schuif rechterpointer naar rechts zolang element niet gevonden in hashset
            // Als wel gevonden in hashset, verschuif links. Ondertussen moeten we de 
            // huidige lengte (hoeft niet per se) en maximum lengte bijhouden
            // Dit doen voor de hele string

            // Voor iedere letter in de string
            for(const char c :s)
            {
                // Beoordeel of deze al in de hashset staat, zo nee, voeg toe, zo ja verschuif linker pointer
                // Per letter beoordelen (if), maar dan voor de hele reeks (dus while, dan if)
                while()
                {
                    
                }
            }


        }
};

int main()
{
    Solution sol;
    string s = "zxyzxyz";
    int answer = sol.LSWRC(s);
    cout<<answer<<endl;
}