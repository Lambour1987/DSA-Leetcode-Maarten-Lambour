#include <iostream>
#include <stdlib.h>
#include <vector>

using namespace std;

// for loop, shiften. Bij normale insertionsort, waarbij de array al gesorteerd is,
// starten we achteraan de array. Dan gaan we steeds een stapje terug in de array.
// Hier is de array nog niet gesorteerd. Dus het eerste element is gesorteerd. 
// Het tweede element moet vergeleken worden met de eerste. Dus vanaf element 2 op
// index 1 gaan we een while loop doen (zolang element )

vector<int> InsertSort(vector<int>&nums)
{
    

}