#1st Approach:
# Time:
# Space:

#2nd Approach:
# Time:
# Space:

#3nd Approach:
# Time:
# Space:


#Ergens opschrijven kanweg:
# zoeken (search),
# sorteren (sort),
# samenvoegen (merge),
# dupliceren (copy),
# vergelijken (compare),
# verschuiven (move in place)
# wisselen (swap),
# verwijder (delete),
# overlsaan (skip)
# partition,
# roteren (rotate),
# achterstevoren (reverse) -> [::-1]
# bijhouden (Keep track of)
# meenemen(carry)
# tellen (count)

#Anders
# dubbele waarde

#Anders2
# Counter
# Carry

# Shift Pointer
# Minimaal
# Maximaal



# Pointers. Two Pointers. Passes. increment, decrement. MEestal while loop
# Hashmap: key, value, count occurances
# Hashset: Counter

#Kanweg: Keuze:
# Forloop
# Doorloop waarden/woorden:     for n in nums
# Doorloop index:               for i in range(len(nums))
# Doorloop index reverse:       for i in range(len(nums)-1,-1,-1): Links: Startwaarde is het eind, Midden is Stopwaarde, rechts is stapgrootte
# Doorloop waarden:             for i, n in enumerate(nums)
# Doorloop letters:             for i in range(len(strs[0])):


#Do While
# Initialiseer pointers:    i, j = 0,0
# Doorloop index:           i = i + 1

#Sliding Window